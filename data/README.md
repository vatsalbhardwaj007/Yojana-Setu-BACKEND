# Data Assets Directory

This directory holds scheme datasets, seed records, and configuration files for the YojanaSetu backend:

- `schemes/`: Verified scheme catalog records (canonical scheme JSON / CSV).
- `rules/`: Externalized eligibility rule definitions and criteria mappings.
- `tutorials/`: Step-by-step application tutorial metadata, instructions, and mock assets.
